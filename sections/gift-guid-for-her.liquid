<div class="page-width">

  <div class="gift-wrapper">
  <div class="git-her">
     
    <div class="carousel-title">
      {% if section.settings.tera_title_collection != blank %}
      <h3>{{ section.settings.tera_title_collection }} </h3>
      {% endif %}
      
      {% if section.settings.carousel_content != blank %}
       <p> {{ section.settings.carousel_content }}</p>
      {% endif %}
      
      {% if section.settings.btn_shop != blank %}
        <a href="{{ section.settings.btn_url }}" class="btn btn-black">
            <span class="ben-text">{{ section.settings.btn_shop }}</span>
        </a>
      {% endif %}

    </div>
     
    
  </div>
    <ul class="degree_product" style="opacity:0;">   
    {% for product in collections[section.settings.product_col].products %}
      <li class=" ">
        {% include 'product-card-grid', max_height: 270, product: product, show_vendor: section.settings.show_vendor %}
        <div class="list-product-price">
          {{product.price | money}}
        </div>
      </li>
      {% endfor %}
      
    </ul>
  
</div>
</div>

  <script>
    $(document).ready(function(){
      var sliderCount = 3;
      if($(window).width() >480 && $(window).width() <767){
        sliderCount = 3;
        } else if ($(window).width() <480) {
          sliderCount = 2;
        } 
      $('.degree_product').slick({
        dots: false,
        arrows:true,
        infinite: true,
        speed:1300,
        slidesToShow:sliderCount,
        slidesToScroll: 1, 
      });
      
      
      
      // loading content
          setTimeout(function(){
          jQuery('.degree_product').css('opacity',1);
          jQuery('.loader_new').hide();
          }, 2000)
    });

  
</script> 

<style>
  .holiday_product_slider{
    padding:0 30px;
    margin:30px 0;
  }
  .dots{
    display:none;
  }
   .carousel-title{
     text-align:center;
      margin-top:30px;
   }
  .degree_product{
    padding:30px 20px 50px;
  }
  .degree_product .slick-prev{
        left:0px;
  }
  .degree_product .slick-next{
        right:0px;
  }
</style>
 
 
{% schema %}
    {
        "name": "prodcut carousel",
        "settings": [
          {
            "type":"text",
            "id":"tera_title_collection",
            "label": "collection title",
            "default":"Gifts for Her"
          },
          {
            "type":"text",
            "id":"carousel_content",
            "label": "section content ",
            "default":"Celebrate her with stylish gifts, designed to keep up with her on-the-go lifestyle. "
          },
          {
            "type":"text",
            "id":"btn_shop",
            "label": "button text",
            "default":"Gifts for Her"
          },
          {
            "type":"url",
            "id":"btn_url",
            "label": "button url"
          },
          {
            "type": "collection",
            "id": "product_col",
            "label": "Select Collection"
          }
        ],
	"presets": [
     {
       "name": "prodcut carousel"
     }
   ]
  }
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}